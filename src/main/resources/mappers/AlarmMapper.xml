<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pikcurchu.pikcur.mapper.AlarmMapper">

    <select id="selectAlarmList" resultType="com.pikcurchu.pikcur.dto.response.ResAlarmDto">
        SELECT
            *
        FROM
            alarm
        WHERE
            member_no = #{memberNo}
        and status_no = '01'
    </select>
    <insert id="insertAlarm">
        insert into
        alarm(member_no, alarm_title, alarm_content)
        values(#{memberNo}, #{alarmTitle} #{alarmTitle})
    </insert>
</mapper>