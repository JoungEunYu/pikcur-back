<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pikcurchu.pikcur.mapper.AuthMapper">

    <select id="authById" resultType="com.pikcurchu.pikcur.vo.Member">
        select
        member_no as memberNo,
        id,
        name,
        authority,
        password
        from
        member
        where
        id = #{id}
    </select>

    <select id="findStatusNo" resultType="boolean">
        select
            status_no
        from
            member
        where id = #{id}
    </select>

    <insert id="insertMember" parameterType="com.pikcurchu.pikcur.vo.Member">
        INSERT INTO member (
        id,
        password,
        name,
        email,
        phone,
        birth,
        gender,
        authority,
        status_type,
        status_no,
        login_date,
        create_date,
        update_date,
        account,
        bank,
        block_list
        )
        VALUES (
        #{id},
        #{password},
        #{name},
        #{email},
        #{phone},
        #{birth},
        #{gender},
        'MEM',
        'MEM',
        '01',
        null,
        NOW(),
        null,
        null,
        null,
        null
        )
    </insert>

    <select id="findIdByEmail" parameterType="string" resultType="String">
        SELECT id FROM member WHERE email = #{email}
    </select>

    <update id="updateMemberToWithdrawal" parameterType="String">
        UPDATE member
        SET status_no = '02',
        update_date = NOW()
        WHERE id = #{id}
    </update>

    <select id="countById" parameterType="String" resultType="int">
        SELECT
            COUNT(*)
        FROM
            member
        WHERE
            id = #{id}
        and (
                status_no = '01'
            or  status_no = '03'
            or  status_no = '04'
        )
    </select>

    <update id="updatePassword" parameterType="map">
        UPDATE member
        SET
            password = #{password}
        ,   update_date = NOW()
        WHERE id = #{id}
    </update>
</mapper>