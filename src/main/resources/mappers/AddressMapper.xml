<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pikcurchu.pikcur.mapper.AddressMapper">
    <insert id="insertAddress">
        INSERT INTO address (
            member_no
        ,   name
        ,   phone
        ,   address
        ,   address_detail
        ,   is_default
        )
        VALUES (
            #{memberNo}
        ,   #{req.name}
        ,   #{req.phone}
        ,   #{req.address}
        ,   #{req.addressDetail}
        ,   #{isDefault}
        )
    </insert>

    <select id="selectAddressIsDefault" resultType="int">
        select
            count(*)
        from
            address
        where
            member_no = #{memberNo}
    </select>

    <select id="selectAddressList" resultType="com.pikcurchu.pikcur.vo.Address">
        select
            address_id
        ,   name
        ,   phone
        ,   address
        ,   address_detail
        ,   is_default
        from
            address
        where
            member_no = #{memberNo}
    </select>

    <update id="updateAddress">
        UPDATE
            address
        SET
            name = #{req.name}
        ,   phone = #{req.phone}
        ,   address = #{req.address}
        ,   address_detail = #{req.addressDetail}
        WHERE
            member_no = #{memberNo}
        and address_id = #{req.addressId}
    </update>

    <delete id="deleteAddress">
        delete from
        address
        where
        member_no = #{memberNo}
        and address_id = #{addressId}
    </delete>

    <update id="updateDefaultAddressN">
        UPDATE
            address
        SET
            is_default = 'N'
        WHERE
            member_no = #{memberNo}
        and is_default = 'Y'
    </update>

    <update id="updateDefaultAddressY">
        UPDATE
            address
        SET
            is_default = 'Y'
        WHERE
            member_no = #{memberNo}
            and address_id = #{addressId}
    </update>
</mapper>
