<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pikcurchu.pikcur.mapper.AccountMapper">

    <insert id="insertAccount">
        INSERT INTO account (
            member_no
        ,   bank
        ,   account_number
        ,   name
        ,   is_default
        )
        VALUES (
            #{memberNo}
        ,   #{req.bank}
        ,   #{req.accountNumber}
        ,   #{req.name}
        ,   #{isDefault}
        )
    </insert>

    <select id="selectAccountIsDefault" resultType="int">
        select
            count(*)
        from
            account
        where
            member_no = #{memberNo}
    </select>

    <select id="selectAccountList" resultType="com.pikcurchu.pikcur.vo.Account">
        select
            account_id
        ,   bank
        ,   account_number
        ,   name
        ,   is_default
        from
            account
        where
            member_no = #{memberNo}
    </select>

    <update id="updateAccount">
        UPDATE
            account
        SET
            bank = #{req.bank}
        ,   account_number = #{req.accountNumber}
        ,   name = #{req.name}
        WHERE
            member_no = #{memberNo}
            and account_id = #{req.accountId}
    </update>

    <delete id="deleteAccount">
        delete from
            account
        where
            member_no = #{memberNo}
        and account_id = #{accountId}
    </delete>

    <update id="updateDefaultAccountN">
        UPDATE
            account
        SET
            is_default = 'N'
        WHERE
            member_no = #{memberNo}
        and is_default = 'Y'
    </update>

    <update id="updateDefaultAccountY">
        UPDATE
            account
        SET
            is_default = 'Y'
        WHERE
            member_no = #{memberNo}
        and account_id = #{accountId}
    </update>

</mapper>